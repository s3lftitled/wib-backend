const LEAVE_STATUS = {
  APPROVED: 'approved',
  PENDING: 'pending',
  REJECTED: 'rejected',
}

const LEAVE_TYPES = {
  SINGLE_DAY: 'single',
  MULTI_DAY: 'multi'
}

module.exports = { LEAVE_STATUS, LEAVE_TYPES }